<!--
この PR で片づく issue があるなら、下の `Closes` に番号を入れてください。
**マージした瞬間に、GitHub がひとりでに閉じます**（人の手で閉じに行かなくてよくなる）。

  片づかない  … `Closes` の行ごと消す
  指すけど閉じない（`docs/island-misses.md` の節番号など）
              … `#147 は閉じない` のように1行書く

`Closes Check` は、**いま開いている issue を指しているのに `Closes` が無い** PR
だけで落ちます。節番号だけを指している PR では落ちません。
-->

Closes #

## 何をしたか

## なぜそうしたか

<!-- 何をしたかではなく、なぜそうしたか。読めば分かることは書かない -->

## 確かめたこと

<!-- 終了コードか、実測の数字。「通った」だけでは確かめたことにならない -->
