/**
 * 手で作った島（`components/island/`）が描いている章。
 *
 * あの島は 1200四方に 300要素を手で置いた1枚もので、いま腰を据えている島
 * （台所も工房も建っている）そのもの。**この章のあいだは、表紙はあちらのまま。**
 * 章が変われば、その島は「その章の島」ではなくなるので、
 * 章から組み上げる島（`components/isle/cover.ts`）に入れ替える。
 *
 * **ここを "use client" のファイルに置かない。** クライアント側のモジュールから
 * サーバ側（`app/page.tsx`）へ値を持ち出すと、文字列ではなく参照が渡ってきて、
 * 比較がいつも false になる。**表紙が丸ごと入れ替わっていたのに、
 * 型でもビルドでも気づけない**（実際に1回そうなった。DOM を数えて見つけた）。
 */
export const HAND_MADE_CHAPTER = "caucasus";
