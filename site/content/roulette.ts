/**
 * 配信のルーレット（#164）で、こちらが書く文。
 *
 * ## 結果のコメントは、ここの1行が決まり
 *
 * 当たりが決まると、あやとのチャンネルで配信のチャットに1行投げる。
 * 投げるのは Cloud Functions（OBS が落ちていても投げられるように）だが、
 * **文面の出どころはここ。** コントローラーが回すときに、この型を
 * サーバーへ渡す。あとから直したくなったら、下の1行だけを直せばいい。
 *
 * `{}` のところに、当たった選択肢がそのまま入る。
 *
 * **ユーザー名は入れない。** あやとが出した文面のとおり（#164 のコメント）。
 * 選ばれた人は、自分の書いたコメントが読まれるので自分だと分かる。
 *
 * **絵文字はこのまま入れる。** 島の画面に絵文字を出さない決まり
 * （`docs/island-design.md` 1章）は、**こちらがサイトに書く字**の話。
 * 配信のチャットに投げるコメントは島の外なので、そこには掛からない。
 * だからこの文字列は、画面に出さないこと。
 */
export const RESULT_SAY = "ルーレットの結果、『{}』に決まりました🎉👏";

/**
 * 結果が出てから、配信にコメントするまでの待ち（秒）。
 *
 * 配信には数秒から十数秒のラグがある。早すぎると、視聴者さんの画面で
 * ルーレットが止まる前に答えが出てしまう。**ラグは日によって変わる**ので、
 * あやとがコントローラーで切り替える（#164 のコメント）。
 */
export const WAIT_SECONDS = [5, 10, 15] as const;

/** 選択肢の上限。いまのルーレットが 36 で打ち止めなので、それに合わせる。 */
export const MAX_ITEMS = 36;

/** コントローラーに出す言葉。ボタンは動作そのものの言葉にする。 */
export const RL_UI = {
  /** 表示側（OBS）が、まだ何も入っていないときに出す1行 */
  standby: "ルーレット準備中",
  /** コントローラーの、コメントがまだ来ていないとき */
  noComments: "コントローラーを開いてから来たコメントが、ここに流れます。",
  /** 配信していないとき。困っているのではなく、事実だけを言う */
  noLive: "いま配信していません。手で足すぶんは、そのまま使えます。",
};
