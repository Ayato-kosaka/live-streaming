"""**走らせないと決めた判定器と、その理由。**

`python/watch_census_selftest.py` がここを読む。**ここに名前の無い判定器が
1本でも「どこからも走らない」なら、棚卸しは 1 で落ちる。**
だから「走らせない」は、**黙って0に畳むのではなく、ここに1行書く**ことになる。

## なぜ表が要るか（2026-09-19）

その日まで、棚卸しは「どこからも走らない見張り: 0本」と言って緑だった。
見分けが名前（`*_selftest.*`）だけで、**そう名付けなかった判定器が
最初から目に入っていなかった**から。中身で見分けるようにしたら、
どこからも走らないものが33本出た。

**33本ぜんぶを CI に載せるのが正しいわけではない。** 絵を撮って人が見る道具は、
機械で回しても合否が出ない。だから「載せる」と「断る」を1本ずつ決めて、
**断ったものは理由をここに残す。** 数えられていないものを0本にする、
というのはそういうこと。

## 書きかたの決まり（守らないと棚卸しが 2 で落ちる）

1. 空にしない
2. 12文字以上（「重い」だけで断らない）
3. **いつ時点の話かの日付**を入れる（`2026-09-19` の形）。
   `docs/island-misses.md` #141 の決めごと6——
   **繋がない理由は、測ってから書く。数字と、いつ測ったかを一緒に置く**

表が腐るのも見ている。**ここに在るのに実は走っている**（断りが古い）、
**ファイルが無い**、**判定器として拾えていない**——どれも 2 で落ちる。

## いま断っていないもの（＝載せた）

- `tools/sprites/rootcheck.mjs` … 0.18秒・依存なし。`selftest.yml` の毎 PR
- `tools/sprites/ogcheck.py` … 6.0秒・Pillow だけ。`selftest.yml` の毎 PR
- `tools/sprites/walkedcount.mjs` / `tools/sprites/dexfit.mjs` …
  書き出しとブラウザが要るので `selftest.yml` の別の job（`browserwatch`）

## 測った値（2026-09-19。この箱。4コア）

| 何 | 実測 |
| --- | --- |
| `next build`（書き出し1本） | 1分10秒 |
| `walkedcount.mjs`（5日 × 7か所） | 2分47秒 |
| `dexfit.mjs`（4幅 × 103マス） | 8.8秒 |
| `rootcheck.mjs` | 0.18秒 |
| `ogcheck.py` | 6.0秒 |
| `tools/sprites` の `npm ci` | 1.2秒（playwright-core ほか2つ） |

## CI で出た実測（2026-09-19。run 35418964922）

| job | 合計 | 中身 |
| --- | --- | --- |
| `selftest` | **196秒** | 見張り50本 127秒 ＋ `ogcheck` 9秒 ＋ 下ごしらえ |
| `browserwatch`（絵を落とし直す回） | **588秒** | 書き出し 42秒 / ブラウザ 19秒 / 絵を落とす 345秒 / `walkedcount` ＋ `dexfit` で 163秒 |
| `browserwatch`（置いてある絵に当たる回） | **247秒** | 絵を落とすところが1秒未満（run 35421444959） |

**絵を落とす 345秒が半分以上**だったので、名簿の JSON の指紋を鍵にして
置きっぱなしにした（絵を差し替えると URL の `token=` が変わるので、
差し替えた日だけ落とし直す）。**置き場は PR ごとに別**なので、
その PR の1回目は必ず落とし直す。

書き出しとブラウザが要る道具は、**この土台の上でしか回らない。**
土台は `browserwatch` に在るので、次に載せるならそこへ足す。
"""

from __future__ import annotations

EXCUSES: dict[str, str] = {
    # ---- 鍵が要る・外に出る（回し役の SKIP と同じ理由） ----
    "python/admin/character_days_probe.py":
        "BigQuery と Firestore を読む下見。鍵が要るので PR では回せない"
        "（2026-09-19。回し役の SKIP と同じ「鍵が要る」）",
    "python/admin/chatter_voices.py":
        "住人の口調を本人のコメントから拾う下ごしらえ。BigQuery の鍵が要り、"
        "出すのは人が選ぶ材料なので機械で合否を出さない（2026-09-19）",
    "python/chat_replay_probe.py":
        "YouTube を1本ずつ叩いて記録の有無を見る。外に出るので PR ごとには"
        "回さない（2026-09-19。回し役の SKIP と同じ「外に出る」）",

    # ---- 部品。単体では走らない ----
    "tools/sprites/route.mjs":
        "撮るときの差し替えを1か所にまとめた部品で、他の道具が import して使う。"
        "単体で回すものではない。合否は借りる側が出す（2026-09-19）",
    "tools/sprites/served.mjs":
        "「開いたつもりで404を測っていた」を止める部品。`reportMissing` を"
        "呼ぶ側が合否を出す。単体では走らない（2026-09-19）",

    # ---- 撮って人が見る（合否を機械で決められない） ----
    "tools/sprites/cardshot.mjs":
        "カードの一覧を撮って人が見る道具。撮れたことは出せることではないので、"
        "機械で合否を出さない（2026-09-19）",
    "tools/sprites/og.mjs":
        "共有画像の下絵を島から撮る道具。絵として出せるかは人が見る。"
        "画素で言える範囲は `ogcheck.py` に切り出して毎 PR で回している（2026-09-19）",
    "tools/sprites/island.mjs":
        "島の寄り・引きを撮って1秒あたりのコマ数を測る。数字は箱の混みぐあいで"
        "2倍3倍に揺れるので、CI の数字は読めない（2026-09-19。CLAUDE.md の実測）",
    "tools/sprites/atlasfund.mjs":
        "次の島の建ちぐあいを4枚撮って人が見る道具。書き出しとブラウザが要る"
        "（書き出しだけで1分10秒。2026-09-19 実測）",
    "tools/sprites/atlashouse.mjs":
        "建設中の家とできあがった家の寸法を突き合わせて人が見る。"
        "書き出しとブラウザが要る（2026-09-19）",

    # ---- 書き出し＋ブラウザが要って、1回が長い ----
    "tools/sprites/crawl.mjs":
        "書き出した131面を一通り歩く。1回4分半で、書き出し1分10秒が別に要る"
        "（2026-09-19）。出す前のチェック（`/island-ship`）で毎回回している",
    "tools/sprites/tapink.mjs":
        "26面 × 4とおりで ひと回り40〜60分（`island-standards.md` §15 の実測）。"
        "PR ごとには載せられない。出す前のチェックで回す（2026-09-19）",
    "tools/sprites/hitbox.mjs":
        "押しどころの実寸。面を手で渡す作りで、単体だと渡し忘れた面が"
        "「割れ0」に化ける。26面まとめて回す `tapink.mjs` 側で見る（2026-09-19）",
    "tools/sprites/inkpx.mjs":
        "字の濃さを画素から測る。同じく面を手で渡す作りで、"
        "26面ぶんは `tapink.mjs` が回す（2026-09-19）",
    "tools/sprites/inkband.mjs":
        "字の濃さを送りながら測る。`tapink.mjs` が子として起こす側なので、"
        "単体では繋がない（2026-09-19）",
    "tools/sprites/shotstable.mjs":
        "同じ面を2回撮って揃うかを見る。`tapink.mjs` が濃さの前に通す子で、"
        "単体では繋がない（2026-09-19）",
    "tools/sprites/popcheck.mjs":
        "押せないのに厚みがあるものを21面ぶん数える。書き出しとブラウザが要り、"
        "出す前のチェックで回す（2026-09-19）",
    "tools/sprites/svgcut.mjs":
        "紙の外で切られている字を全面から数える。書き出しとブラウザが要る"
        "（2026-09-19）",
    "tools/sprites/negclaim.mjs":
        "「無い・していない」の言い切りを全面から洗い出す。**どれが嘘かは"
        "機械では決められない**——並べるところまでが仕事なので、赤にしない"
        "（2026-09-19。#143 の決めごと3）",
    "tools/sprites/zeroclaim.mjs":
        "「読めていない」を0と言い切っている場所を洗い出す。同じく並べるまでが"
        "仕事で、人が読んで決める（2026-09-19）",
    "tools/sprites/orphan.mjs":
        "看板の一言が最後の1文字だけ次の行に落ちていないかを測る。"
        "書き出しとブラウザが要る（2026-09-19）",
    "tools/sprites/jsjump.mjs":
        "画面が出た瞬間の札の伸びを測る。跳ねの px は箱の混みぐあいで揺れるので、"
        "CI の数字は読めない（2026-09-19）",
    "tools/sprites/stateagain.mjs":
        "「もう一度よみこむ」を押して読み直るかを見る。本番の口を叩くので"
        "PR ごとには回さない（2026-09-19）",
    "tools/sprites/prderr.mjs":
        "出したものを開いて JS の例外を数える。**配ったあと**を見る道具なので、"
        "配る前の PR では見るものが無い（2026-09-19）",
    "tools/sprites/isletalk.mjs":
        "島の吹き出しを住人1人ずつ押して測る。島を触ったときに回すもので、"
        "書き出しとブラウザが要る（2026-09-19）",
    "tools/sprites/facecheck.mjs":
        "看板と顔が同じ人かを見る。住人の絵を先に落としておかないと"
        "全員おなじ顔で通ってしまう。落とす先が本番なので PR では回さない"
        "（2026-09-19）",
    "tools/sprites/iconcheck.mjs":
        "じぶんのことの絵がほんとうに描けたかを見る。`facecheck.mjs` と同じく、"
        "本番から絵を落とさないと数字が嘘になる（2026-09-19）",
    "tools/sprites/dexdays.mjs":
        "図鑑の札の「いっしょにいた日数」を1枚ずつ開いて見る。札の中身は"
        "本番の口から来るので、PR の書き出しでは空になる（2026-09-19）",
    "tools/sprites/dexspot.mjs":
        "図鑑の札の「今日いるところ」を1枚ずつ開いて見る。`dexdays.mjs` と同じく"
        "本番の口が要る（2026-09-19）",
}
