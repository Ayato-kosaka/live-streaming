# YouTube アーカイブチャット取得システム - Python依存パッケージ
#
# インストール方法:
#   pip install -r requirements.txt
#
# 説明:
#   - google-cloud-bigquery: BigQuery クライアント（videos管理、chat_messages MERGE）
#   - requests: HTTP通信（将来の拡張用、現在は未使用）

# BigQuery 操作
google-cloud-bigquery>=3.11.0

# YouTube Data API v3
google-api-python-client>=2.100.0

# HTTP通信（オプション、将来の拡張用）
requests>=2.31.0

# Firestore（あやと島の数字の書き込み）
google-cloud-firestore>=2.16.0

# Cloud Storage（退避。旅の写真の実体と、書き出したものの置き場）
# python/backup/ が使う。requirements を分けないのは、run_admin_script.yml が
# ここしか入れないため（下見も戻しも、あの入口から回す）
google-cloud-storage>=2.14.0
