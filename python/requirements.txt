# YouTube アーカイブチャット取得システム - Python依存パッケージ
#
# インストール方法:
#   pip install -r requirements.txt
#
# 説明:
#   - google-cloud-bigquery: BigQuery クライアント（videos管理、chat_messages MERGE）
#   - requests: HTTP通信（将来の拡張用、現在は未使用）

# BigQuery 操作
google-cloud-bigquery>=3.11.0

# YouTube Data API v3
google-api-python-client>=2.100.0

# HTTP通信（オプション、将来の拡張用）
requests>=2.31.0

# Firestore（あやと島の数字の書き込み）
google-cloud-firestore>=2.16.0

# Cloud Storage。**2つの用が同じ1本に乗っている。**
#   退避（python/backup/）— 旅の写真の実体と、書き出したものの置き場
#   キャラクターの絵（#284）— 移行の下見で置き場を数える
# requirements を分けないのは、run_admin_script.yml がここしか入れないため
google-cloud-storage>=2.14.0

# 画像の縮小（キャラクターの絵を幅ごとに焼く）。
# **ドライブは `=s256` を勝手に作ってくれていたが、Storage はやってくれない。**
Pillow>=10.0.0
